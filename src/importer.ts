// @ts-ignore - this error should not be here, and this type should be imported in .d.ts
import type { exporter } from "./exporter.js";


/**
 * this is some JSDoc.
 * 
 * Note how this link is rendered when hovering over the name `importer` below.
 * @see {@link exporter} for more info (or some other reason to reference external object)
 */
export const importer = "importer";
